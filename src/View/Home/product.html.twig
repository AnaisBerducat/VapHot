{% extends 'layout.html.twig' %}
{% block title %} - Product {{article.title}}{% endblock %}
{% block content %}

<div class="bgproducts">
    <br>       
        <div class="text-color">
            <h1>{{ article.title }} 📦</h1>
        </div>
    <br>
    <div class="container text-center">
        <div class="row justify-content-around custom-line">
            <div class="col-lg-3 col-md-6 col-xs-12"> 
                <div class="card mb-4 shadow p-3 mb-5 bg-white rounded">                    
                <img src={{ article.image }} class="card-img-top">
                    <div class="card-body">
                        <h5 class="card-title">{{ article.title }}</h5>
                        <p class="card-text">{{ article.description }}</p>
                        <p class="card-text">{{ article.price }} $ </p>
                        <div class="text-center">
                            <div class="d-grid gap-2 d-md-block">
                                <a href="/home/like/{{article.id}}" class="btn btn-success btn-sm">Add to Fav</a>
                                <a href="/home/addToCart/{{article.id}}" class="btn btn-primary btn-sm">Add to Cart</a>
                            </div>
                        </div>
                    </div>
                </div>                
        </div> 

        </div>
    </div>


</div>

{% endblock %}