{% extends 'layout-admin.html.twig' %} {% block title %} Add Category{% endblock %}
{% block content %}
<div class="container p-5">
    <form method="POST">
    
        {% if errors is not empty %}
            {% for error in errors %}
            <p>{{error}}</p>
            {% endfor %}
        {% endif %}
        <div class="mb-3">
            <label for="name" class="form-label">Name</label>
            <input type="text" class="form-control" id="name" name="name" value="{{ category.name }}">
        </div>
    
        </select>
        <div class="mt-3 text-center">
            <button type="submit" class="btn btn-primary">Add/Change this category</button>
        </div>    
        
    </form>
</div>

{% endblock %}